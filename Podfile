# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'TattoodoAssignment' do
  use_frameworks!
  pod 'Kingfisher', '~> 5.0'
  pod 'Swinject'	
  target 'TattoodoAssignmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
